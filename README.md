# rubrica-contatti
Semplice applicazione CRUD per la gestione di una rubrica contatti, costruita con HTML, CSS e JavaScript puro. Utilizza fetch per le chiamate API e il localStorage per la persistenza dei dati.
